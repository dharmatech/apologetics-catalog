# Apologetics Viewpoint Catalog

A neutral catalog and compiler for structured apologetics viewpoints.

The project stores human-authored YAML content, validates it, and will generate
derived outputs such as normalized JSON, graph data, and human-readable views.
